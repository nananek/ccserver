{
  "name": "ccserver",
  "version": "1.0.0",
  "description": "Context & Coordination Server for browser-based AI CLI sessions",
  "private": true,
  "type": "module",
  "//engines": "下限はサポートする最古の LTS (22.13 = Node 22 が LTS に入った版)。上限は置かない: engine-strict な環境で新しい Node の install を止めてしまうため。CI が実際に検証している範囲は .github/workflows/ci.yml のマトリクス (22/24/26) を参照。",
  "engines": {
    "node": ">=22.13"
  },
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "npm run dev:server & npm run dev:client",
    "dev:server": "node --watch server/index.js",
    "dev:client": "npm run dev --workspace=client",
    "test": "npm run test --workspace=server",
    "test:e2e": "playwright test",
    "login-token": "node server/cli/issue-login-token.js",
    "setup": "node server/cli/setup.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  },
  "dependencies": {
    "terser": "^5.46.0"
  }
}
